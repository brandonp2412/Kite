# Kite performance contract

Kite treats visible jitter as a correctness bug, not a polish issue.

The contract is pinned in `lib/benchmark/performance_contract.dart` and independently asserted by `test/performance_contract_test.dart`. Weakening a threshold therefore requires an explicit code change and an explicit pin-test change.

## Required invariants

- Opening a user chat must produce zero Flutter build-frame budget violations.
- Opening a user chat must produce zero Flutter raster-frame budget violations.
- On physical Android hardware, opening a user chat must produce zero end-to-end frame-budget violations.
- Returning from representative settings routes with back navigation must produce zero Flutter build-frame and raster-frame budget violations; physical Android also gates end-to-end frame timing.
- On Waydroid, end-to-end `totalSpan` is recorded but is not a merge gate because host/compositor scheduling is outside Kite; build and raster budgets remain strict zero-violation gates.
- The deterministic 120 Hz motion test must produce zero unintended geometry movement in the sidebar, chat panel, message list, and composer.
- The canonical fixture stays at 200 rooms and 100 messages per room unless this contract and its pin test are deliberately revised.
- The dedicated room-list scroll benchmark uses a separate deterministic 3,000-room fixture; it must not replace or silently resize the canonical 200-room fixture.
- Timeline performance journeys use a separate deterministic 1,200-message fixture spanning text, formatted, image, file, audio, poll, and location event shapes.
- Pagination adds a deterministic 100-event page; incoming-message insertion and reaction/read-receipt/typing updates are measured as independent mutation journeys.
- Offline recovery renders the canonical 200 cached rooms continuously while a deterministic 20-room recovered sync batch is applied; room ordering and the first visible row geometry must remain unchanged.
- The presentation-cache retention soak switches across 24 rooms for 40 cycles while replaying the same 40-event pagination page; the retained set must stay at exactly 2,400 unique timeline events and unchanged timeline signal/list identities after the first page is applied.
- The decoded Flutter image cache is capped at 96 entries and 32 MiB so full-resolution media cannot grow the process cache without an eviction bound.
- The canonical warm-switch benchmark remains 30 chat opens.
- The benchmark detector must prove itself by failing when the 40 ms artificial build stall is enabled.

## Required command before marking roadmap work complete

```sh
tool/quality_gate.sh
```

The quality gate performs `flutter analyze`, the deterministic Flutter tests, the Waydroid positive/negative/positive jitter sequence, and the settings back-navigation profile benchmark. All repository-driven Waydroid performance runs must use `tool/with_waydroid_lock.sh`, which holds an exclusive per-device `flock` for the full install/run cycle so concurrent workers cannot replace the benchmark package mid-run. `tool/verify_jitter_harness.sh` and `tool/verify_back_navigation_harness.sh` acquire that lock automatically.

The expected jitter-harness sequence is:

```text
clean benchmark      PASS
40 ms injected stall FAIL
clean benchmark      PASS
```

A benchmark that cannot detect the injected fault is itself considered broken.

## Authoritative physical-device gate

Waydroid is the continuous regression detector. Release candidates must additionally run the Android Macrobenchmark on fixed physical hardware. A physical-device result is authoritative for end-to-end presentation timing; Waydroid results are not used to claim real-device jank freedom.

## Evidence format

`integration_test/open_dm_performance_test.dart`, `integration_test/release_journey_performance_test.dart`, `integration_test/timeline_release_performance_test.dart`, `integration_test/room_list_performance_test.dart`, `integration_test/media_viewer_performance_test.dart`, and `integration_test/app_startup_performance_test.dart` emit machine-readable frame data including refresh rate, frame budget, build/raster/total-span violations, worst timings, and raw per-frame timings. Release-journey output includes room-list scroll, composer keyboard, mixed rich timeline scroll, pagination, incoming-message insertion, and reaction/read-receipt/typing mutation records. The dedicated timeline target runs those four timeline journeys alone so they can be reverified without holding the shared Waydroid device for unrelated release journeys. The dedicated room-list target covers search/filter and Spaces/Sections switching against the deterministic 3,000-room fixture. The media-viewer target covers repeated open/close, flick-to-dismiss, and adjacent-media swipe against deterministic media. Keep this output when investigating any failure.

The 2026-09-15 Waydroid profile-mode release-journey run used a 16,666 µs frame budget and recorded zero build, raster, and total-span violations for both new journeys. The 3,000-room scroll captured 117 frames with worst build/raster/total-span times of 4,959/2,831/6,661 µs. Composer keyboard appearance captured 7 frames with worst build/raster/total-span times of 3,365/2,152/6,366 µs. Waydroid still gates only build and raster timing as described above.

A later 2026-09-15 Waydroid profile run verified the new mutation benchmarks independently: pagination captured 1 frame at 1,099/1,623/3,209 µs worst build/raster/total-span, incoming-message insertion captured 1 frame at 3,290/1,920/6,050 µs, and 20 reaction/read-receipt/typing updates peaked at 1,204/1,787/3,410 µs. All three had zero build and raster budget violations. Mixed-rich timeline scrolling initially reproduced frame-0 raster violations at 17,636, 27,353, and 22,901 µs with the unchanged 16,666 µs budget. The cause was the benchmark gesture: because the timeline is reversed, it flung toward the already-reached newest-message edge first and measured overscroll rendering instead of timeline scrolling. Correcting the gesture to move toward older events first, while restoring the original rounded row rendering and keeping every threshold unchanged, produced 146 measured frames with zero build, raster, and total-span violations; worst build/raster/total-span timings were 2,978/2,385/5,288 µs. A widget contract now asserts that the benchmark's first fling actually advances the reversed timeline scroll position.

A 2026-09-16 serialized rerun used the new timeline-only target with the same 16,666 µs budget and zero allowed build/raster violations. Mixed-rich scrolling captured 149 frames with worst build/raster/total-span timings of 3,074/11,494/15,938 µs; pagination captured 1 frame at 1,204/1,445/3,009 µs; incoming-message insertion captured 1 frame at 3,714/1,462/5,525 µs; and 20 reaction/read-receipt/typing updates peaked at 1,087/1,333/2,686 µs. All four journeys recorded zero build, raster, and total-span violations. The mixed-event scroll performs one unmeasured traversal before sampling, matching Kite's existing steady-state scroll benchmark policy, and deterministic 120 Hz coverage now asserts that pagination, insertion, and ephemeral updates keep the benchmark list shell and scroll offset fixed. Thresholds are unchanged.

The 2026-09-16 serialized Waydroid profile rerun for room-list mutations passed both journeys at the unchanged 16,666 µs budget. Search/filter switching captured 5 frames with worst build/raster/total-span timings of 5,836/1,979/8,119 µs. Spaces/Sections switching captured 5 frames at 7,211/2,294/9,936 µs. Both recorded zero build, raster, and total-span violations. The deterministic room-list benchmark surface contract also passed before the device run.

The 2026-09-15 Waydroid profile run for media-viewer interactions passed all three journeys with zero build and raster budget violations at the 16,666 µs budget. Three repeated open/close cycles captured 24 frames with worst build/raster/total-span timings of 3,107/15,814/19,039 µs; the single end-to-end total-span miss is recorded but is not a Waydroid merge gate. Flick-to-dismiss captured 3 frames at 1,498/14,564/15,412 µs, and adjacent-media swipe captured 60 frames at 2,064/1,654/3,675 µs with zero build, raster, and total-span violations.

The 2026-09-15 Waydroid profile run for offline recovery passed with the canonical 200 cached rooms kept mounted while 20 deterministic recovered-sync summaries were applied. It captured 1 frame with worst build/raster/total-span timings of 2,975/1,309/4,579 µs at the 16,666 µs budget, with zero build, raster, and total-span violations; the room order and first visible row geometry were unchanged.

A 2026-09-16 serialized revalidation kept every threshold unchanged. The call-screen target passed incoming accept, remote end, and active-control transitions with zero build/raster violations; the worst sampled build/raster/total-span timings were 3,969/4,158/8,542 µs. Offline recovery also passed again with 1 measured frame at 2,382/2,711/5,383 µs while preserving cached room order and first-row geometry. The same run deliberately left three nearby Milestone 15 boxes open because their strict gates reproduced failures: thread media open hit a 17,804 µs raster frame, the media-viewer target hit 20,707 µs on flick-to-dismiss and 18,645 µs on timeline-media open, and app-root startup hit a 32,046 µs first-frame build. No frame budget or allowed-violation count was changed.

The 2026-09-15 deterministic release-reliability pass verified the memory-growth soak across 960 repeated room-switch/pagination operations without increasing the 2,400-event retained set or replacing stable timeline signal/list values. The media-cache pressure test decoded twelve real 1024×1024 images (48 MiB of nominal RGBA content) through Flutter's `ImageCache` and verified LRU eviction kept retained decoded media at or below Kite's 32 MiB / 96-entry limits. The repository quality gate then passed all 206 deterministic tests plus the required Waydroid PASS → injected-40-ms FAIL → PASS jitter sequence. The memory-growth soak therefore has complete deterministic and repository-gate evidence and is closed in Milestone 15.

The 2026-09-15 app-root startup benchmark first exposed a real first-frame build miss: 24,353 µs build / 7,046 µs raster against a 16,666 µs Waydroid budget. The canonical fixture was constructing all 20,000 messages for 200 rooms during that first build even though only one room was visible. Generating and caching only the selected room's 100 deterministic messages, while caching and prewarming Kite theme data before `runApp`, reduced the verified lane-5 run to 12,137 µs build / 5,632 µs raster with zero build and raster violations. Its 18,063 µs total-span miss is recorded but remains non-gating on Waydroid. The same pass verified 12 repeated Alice/Bob login → account-switch → logout cycles: all 24 SDK boundaries closed exactly once and both runtime/store registries returned to zero after every cycle. The repository quality gate then passed all 208 deterministic tests plus PASS → injected-40-ms FAIL → PASS.

The cold-start Android Macrobenchmark remains intentionally unchecked and distinct from the profile-mode Flutter app-root frame benchmark above. Its benchmark target built and launched on Waydroid, but the 10-iteration `CompilationMode.Full` run remained at 0/1 completed until the bounded 5-minute command expired. The iteration count and compilation mode were not reduced to manufacture a pass.

A dedicated `timeline_mutation_performance_test.dart` now covers the first new-message insertion and deterministic send settlement with the same strict profile-mode harness. Its first Waydroid run caught 1 raster-budget violation across 2 sampled frames, worst 19,240 µs, so the Milestone 15 new-message-insertion box remains open.

`room_members_performance_test.dart` now adds deterministic 200-member search/filter coverage alongside the existing member-list scroll journey. On Glass Waydroid the unchanged 16,666 µs raster gate rejected both journeys: the baseline scroll recorded 13 violations across 78 frames with a 20,803 µs worst raster frame, while search/filter recorded 3 violations across 3 frames with a 19,215 µs worst raster frame. A render-tree isolation experiment improved the search sample to 1/3 violations at 18,648 µs but worsened scrolling to 27/78 at 21,700 µs, so that experiment was reverted and no performance checkbox was closed.

A native Android cold-start Macrobenchmark now uses `StartupTimingMetric`, `StartupMode.COLD`, full compilation and ten iterations against an isolated `nz.presley.kite.benchmark` build. The benchmark target and instrumentation are now aligned on debug signing: the module compiles, installs, launches Kite and begins Perfetto capture on Glass Waydroid. The first iteration cannot produce accepted timing evidence there, however: Android accessibility never exposes the expected `Chats` node within the five-second readiness window, and the Waydroid trace environment then reports unsupported external-storage attributes plus a missing `su` command before the guarded run is aborted. The startup item therefore remains open; no timing result is claimed from this virtualized run, and the physical-device Macrobenchmark remains the authoritative release gate.

## Regression policy

Do not raise frame budgets, allow non-zero violations, reduce the number of benchmark iterations, disable the artificial-jitter negative control, or remove geometry checks merely to make a failing change pass. Fix the regression instead. Any intentional contract revision must be documented in the same change with the reason and before/after benchmark evidence.

The 2026-09-15 first profile-mode Thread 2.x benchmark run on Glass Waydroid kept the pinned 16,666 µs budget unchanged. Opening a thread is not release-ready: 1 of 4 sampled frames missed the raster budget, with worst build/raster/total-span timings of 2,038/37,648/39,827 µs. Thread pagination itself passed with 3 sampled frames, zero build/raster/total-span violations, and worst timings of 1,122/9,557/10,856 µs. The driver connection disposed while the remaining thread cases were starting, so retry/subscription/send journeys were not claimed from that run.

A later fully serialized rerun completed all six thread journeys and confirmed the blocker instead of weakening it. Opening a thread missed 3/4 raster frames with a 46,205 µs worst frame; pagination missed 2/3 at 21,263 µs; notification follow/unfollow missed 5/7 at 21,005 µs. Retry, reply send, and focused-thread deep-link open passed the build/raster gate in that baseline run. A `RepaintBoundary` around the sliding route was then tested under the same lock and made the result materially worse, causing five of six journeys to fail; that experiment was reverted. Milestone 15 thread open/switch therefore remains open.

The new-message insertion journey was also rerun twice under exclusive Waydroid ownership. The unchanged benchmark missed 1/2 raster frames with a 23,285 µs worst frame. A diagnostic attempt to settle the already-open IME before measurement still missed 2/2 raster frames at 22,093 µs, proving keyboard settling was not the cause; the harness-only experiment was reverted and the original benchmark remains intact.

The final repository quality-gate attempt passed the release audit, `flutter analyze`, all 333 deterministic Flutter tests, and the golden-focused suite had already passed 51/51 tests, but the required first clean Waydroid jitter-harness leg failed multiple strict raster checks and therefore stopped before the injected-jitter sequence. To isolate the new timeline semantics change, the same open-DM profile target was run from its parent commit under the shared Waydroid lock; the parent also failed broadly, including a 36,624 µs cold-open raster frame and a 92,037 µs reply-composer raster frame. This establishes that the current Waydroid slowdown predates the accessibility commit, but it is still a merge/readiness blocker: no affected roadmap runtime checkbox is closed and the gate remains strict.

A 2026-09-16 thread rerun used the canonical Waydroid harness mode (`KITE_VIRTUALIZED_BENCHMARK=true`), preserving the 16,666 µs frame budget while gating build and raster timings and recording end-to-end total span. All eight measured thread journeys passed with zero build and raster violations. Thread open peaked at 10,693/13,776/25,041 µs build/raster/total-span, with its single total-span miss remaining non-gating on Waydroid by contract. Pagination peaked at 2,826/4,867/5,509 µs, failed-reply retry at 654/1,722/2,966 µs, notification toggle at 1,279/1,682/3,461 µs, reply send at 2,142/2,130/4,604 µs, attachment preview/send at 3,539/3,162/6,316 µs, thread-media open at 2,298/2,161/4,024 µs, and focused-thread destination open at 2,315/2,438/4,952 µs. The Milestone 15 `Thread open/switch benchmark` box remained open at that point because this target verified thread opening and related mutations but did not yet exercise an explicit thread-to-thread switch journey.

A later 2026-09-16 lane-5 pass added an explicit deterministic thread-to-thread route replacement to the same profile target. The first serialized run caught a pre-existing thread-open raster miss at 17,479 µs while the new switch journey itself passed at 4,002/1,379/5,574 µs build/raster/total-span. A second fully serialized rerun passed all nine thread journeys with zero build/raster violations: thread open peaked at 6,081/13,443/19,800 µs and the new switch journey at 3,068/2,467/4,237 µs. The switch benchmark therefore closes with thresholds unchanged. The media-viewer target was also revalidated immediately afterward and all five journeys passed again, including repeated open/close at 3,283/9,352/10,185 µs and flick-to-dismiss at 789/14,861/15,668 µs. Its roadmap box stays open because the bounded full repository quality gate did not reach its final PASS before the 5-minute command limit; it had passed release audit, Rust bridge checks, analysis, ABI smoke tests, and all 796 deterministic Flutter tests before entering the locked Waydroid jitter phase.

A 2026-09-16 lane-5 media-viewer retune kept the same 16,666 µs frame budget while avoiding concurrent full-resolution painting during the Hero flight, caching the moving media plane, making chrome removal immediate during vertical dismissal, and removing the redundant whole-screen fade/final dismiss offset. The serialized Waydroid profile target then passed all five media journeys with zero build, raster, and total-span violations: repeated open/close peaked at 3,811/13,369/14,600 µs build/raster/total-span, flick-to-dismiss at 1,507/15,310/16,151 µs, adjacent swipe at 3,029/2,200/5,618 µs, save feedback at 1,233/2,925/4,461 µs, and timeline-media open at 2,699/3,148/5,018 µs. The media-viewer roadmap box remains unchecked until the full repository quality gate reaches its final PASS.

The same lane removed an unnecessary normal-motion `ThemeData.copyWith` during app-root composition, but startup remains a strict blocker. Two serialized Waydroid profile attempts still recorded one first-frame build violation each, first at 41,945 µs and then at 27,302 µs against the unchanged 16,666 µs budget. The app-startup roadmap box therefore remains open.

A later 2026-09-16 Nox revalidation kept every threshold unchanged. The dedicated media-viewer profile target again passed all five journeys with zero build/raster violations: repeated open/close peaked at 4,411/9,483/10,785 µs build/raster/total-span, flick-to-dismiss at 1,358/16,585/17,388 µs, adjacent swipe at 3,151/2,438/5,905 µs, save feedback at 1,267/2,768/4,165 µs, and timeline-media open at 2,688/2,457/4,661 µs. The single flick total-span miss is non-gating on virtualized Waydroid. Sixteen focused memory/media/motion/golden tests passed, followed by the full deterministic suite at 796 passes with 3 intentional skips. A separately serialized jitter harness completed clean PASS → injected-40-ms FAIL → clean PASS. The repository `quality_gate.sh` rerun nevertheless failed its first clean Waydroid leg on the cold-open DM frame at 18,132 µs build / 18,234 µs raster against the pinned 16,666 µs budget, so the media-viewer and app-startup roadmap boxes remain open and no threshold was weakened.
